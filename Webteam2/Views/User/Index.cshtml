@model Webteam2.Models.UserLists

@{
    ViewData["Title"] = "Index";

}

<br />
<h2 class="text-info">Users List</h2>
<br />

<div class="cust_container row p-0 m-0">
    
    <div class="col-12 border p-3 mt-3">
        <form class="float-left">
            <p>
                <input type="text" asp-for="SearchString" placeholder="Filter Users" />
                <input type="submit" value="Filter"  />
            </p>

        </form>
        <table id="users_table" class="table table-striped border text-left">
            <thead>
            <tr>
                <th class="col-auto">First Name</th>
                <th class="col-auto">Last Name</th>
                <th class="col-auto">Profile</th>
                <th class="col-auto">Role</th>
            </tr>
            </thead>
            <tbody>
            @foreach (var User in Model.Customers)
            {
                <tr>
                    <td> @User.FirstName</td>
                    <td> @User.LastName</td>
                    <td>
                        <a asp-controller="Profile" asp-action="Details" asp-route-id="@User.Id">Profile</a>
                    </td>
                    <td>Customer</td>
                </tr>
            }
            @foreach (var User in Model.ValidatedContractors)
            {
                <tr>
                    <td> @User.FirstName</td>
                    <td> @User.LastName</td>
                    <td>
                        <a asp-controller="Profile" asp-action="Details" asp-route-id="@User.Id">Profile</a>
                    </td>
                    <td>Contractor</td>
                </tr>
            }
            </tbody>
        </table>
    </div>
</div>

